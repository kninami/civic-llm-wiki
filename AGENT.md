# AGENT.md — 시민사회 자동화 도구 위키

시민사회단체가 각자 만든 **자동화 도구**를 모아, 다른 단체가 보고 **자기 조직에 옮겨 심을 수 있게** 하는 위키입니다.
이 파일은 LLM 에이전트를 위한 규칙서입니다. 위키 본문은 LLM이 쓰고, 사람은 자료를 모으고 질문을 던집니다.

## 구성

```
civic-ai-guideline/
├── AGENT.md              ← 이 파일. 규칙
├── raw/                  ← 원본. LLM은 읽기만 하고 절대 수정하지 않는다
│   └── (PDF, 코드, 스크린샷, 인터뷰, 회의록 등 받은 그대로)
└── wiki/                 ← LLM이 쓰고 유지한다
    ├── index.md             카탈로그. 질문에 답하기 전 먼저 읽는다
    ├── log.md               시간순 작업 기록. append-only
    ├── guardrails.md        AI 활용 원칙과 주의사항 (raw/의 두 가이드를 통합)
    ├── tools/               도구 1개 = 파일 1개. 개요·개념·작동방식·코드·사용법·사례를 한 파일에
    │                        바로 실행할 수 있는 도구는 스크립트를 같은 폴더에 나란히 둔다
    └── concepts/            용어사전. 활동가 눈높이로 한 개념 = 한 파일
```

폴더는 `tools/`와 `concepts/` 둘뿐입니다. 카테고리를 더 쪼개고 싶어지면 대신 `tools/` 파일 안의 절로 만드세요.

## wiki/tools/ — 도구 페이지

파일명은 `<단체>-<도구>.md`. 한 도구의 모든 것이 이 한 파일에 들어갑니다.

```markdown
---
tool: 뉴스레터 요약 도구
org: 참여연대
status: 운영중              # 아이디어 | 시범 | 운영중 | 중단
task: 뉴스레터 작성
stack: [Google Apps Script, OpenAI API]
ai_role: 생성형AI-보조       # 생성형AI-핵심 | 생성형AI-보조 | 비생성형AI | AI-미사용
data: 내부                  # 공개 | 내부 | 민감 | 개인정보포함
portability: 보통           # 쉬움 | 보통 | 어려움
skill: 스프레드시트+약간의 코드  # 코딩불필요 | 스프레드시트+약간의코드 | 개발자필요
cost: 월 2만원
updated: 2026-09-02
---

# 도구 이름
> 한 줄 요약.

## 개요
어떤 활동 현장의 어떤 반복 노동을 없애려 했나. 만들기 전에는 어떻게 했나. 누가 얼마나 자주 쓰나.

## 알아야 할 개념
[[concepts/…]] 링크 + 한 줄 설명. 처음 보는 사람 기준으로.

## 작동 방식
입력 → 처리 → 출력. 되도록 mermaid 흐름도.
**사람이 개입하는 지점을 반드시 명시**한다. 없으면 "없음"이라고 쓴다.

## 코드
- 원본 위치: `raw/…` 또는 공개 저장소 링크
- 핵심 부분 발췌 (30줄 이내, 무엇을 하는지 주석)
- 필요한 계정·서비스·권한

## 사용법 — 우리 단체에 옮겨 심기
- 사전 준비 (계정, 예산, 내부 승인)
- 단계 1) 2) 3)
- 예상 소요: 반나절 / 이틀 / 일주일
- 여기서 막힌다: 알려진 함정
- 조직 규모나 업무 환경이 다르면 무엇을 바꿔야 하나

## 주의사항
[[guardrails]] §5 점검표를 채운다. 해당 없는 항목은 뺀다.
| ID | 이 도구에서의 위험 | 완화 장치 | 남은 위험 |

## 사례
같은 파일 안에 시간순으로 계속 덧붙인다.

### [2026-09-02] 참여연대 — 최초 제작
왜 만들었나, 얼마나 썼나, 실제로 뭐가 달라졌나, 사고나 실패는 없었나.

### [2026-10-15] 빠띠 — 이식
무엇을 바꿨나, 얼마나 걸렸나, 잘된 점과 안 된 점.
여기서 나온 함정은 위의 "여기서 막힌다"에도 반영한다.
```

## wiki/concepts/ — 용어사전

파일명은 개념 이름 그대로 (`RAG.md`, `웹훅.md`). 짧게 유지합니다.

```markdown
# 개념 이름
한 문단 정의. 전문용어 없이.

**활동가에게 왜 중요한가** — 실무에서 이게 걸리는 지점.
**흔한 오해** — 있으면.
**관련** — [[다른개념]], 이 개념을 쓰는 도구들.
```

## wiki/index.md · wiki/log.md

`LLM Wiki Architecture.md`의 "Indexing and logging" 절을 따릅니다. 목적이 다른 두 파일입니다.

**index.md — 내용 중심 카탈로그.** 위키에 무엇이 있는지의 목록. 도구 표, 개념 목록, guardrails 절 지도, 원본 목록, 그리고 **"아직 없는 것"**. 질문에 답할 때 이 파일을 먼저 읽고 관련 페이지를 고른 뒤 그 페이지로 들어갑니다. 도구를 등재하거나 개념을 만들 때마다 갱신합니다.

**log.md — 시간순 기록.** 무엇을 언제 했는지. append-only이며 **지우지 않고 덧붙이기만** 합니다. 최신 항목이 위로 옵니다. 첫 줄 형식을 지켜야 grep으로 파싱됩니다.

```
## [YYYY-MM-DD] <작업> | <제목>
```

작업 종류 — `ingest` / `adopt` / `query` / `lint` / `schema`

```bash
grep "^## \[" wiki/log.md | head -5     # 최근 5개
```

기록할 때는 **무엇을 했는지보다 무엇을 판단했는지**를 남깁니다. 나중에 그 판단을 뒤집을 사람이 근거를 볼 수 있어야 합니다.

## wiki/guardrails.md

`raw/`의 『시민사회를 위한 생성형 AI 가이드』(디지털정의네트워크·빠띠·정보인권연구소, 2025.12)와 참여연대 사례발표(2026.04)를 통합한 문서입니다. 기본 원칙 P1–P6, 활용 지침 G1–G6, 조직 절차 V1–V7, 미해결 쟁점, 도구 점검표로 구성됩니다.

이 문서를 고칠 때는:
- 출처 표시(`[가이드]` / `[참여연대]` / `[확장]`)를 반드시 유지한다. **원본에 없는 말을 원본인 척 쓰지 않는다.**
- 새 원본이 `raw/`에 들어오면 새 표시를 만들어 통합한다.
- 원본 가이드나 관련 법(인공지능기본법 등)이 개정되면 전체를 재검토한다.

## 작업 절차

**도구 등재**
1. `raw/`의 원본을 읽는다. 코드가 있으면 코드를 읽는다. README만 읽고 작동방식을 쓰지 않는다.
2. **등재 전 스크리닝** — API 키·토큰·`.env`·서비스 계정 파일, 실명/연락처가 든 샘플 데이터, 회원·후원자 정보, 회의록·피해자 인터뷰·회계 자료, 스크린샷 속 개인정보. 발견하면 **지우지 말고 사람에게 보고**한다.
3. 사람과 요점을 대화한다 — 특히 "이 도구의 가장 위험한 지점이 뭔가".
4. `wiki/tools/`에 위 규격으로 쓴다. **주의사항 표를 반드시 채운다.**
5. 새 용어가 나오면 `wiki/concepts/`에 페이지를 만들고 상호 링크한다.
6. `wiki/index.md`의 도구 표에 한 줄 추가하고, 해당하면 "아직 없는 것" 목록에서 지운다.
7. `wiki/log.md` 맨 위에 `## [날짜] ingest | 도구명` 항목을 추가한다.

**질문에 답할 때**
- `wiki/index.md`를 먼저 읽어 관련 페이지를 고른다. 그 다음 해당 페이지와 `wiki/guardrails.md`를 근거로 답하고 `[[페이지]]`를 인용한다. 위키에 없으면 "위키에 없음"이라고 말한다.
- "X를 자동화하고 싶다"는 질문에는 **주의사항 검토를 답에 포함**한다. 특히 G6-③ — 생성형 AI 없이 되는 일이면 그쪽을 권한다.

**점검** (가끔) — 결과는 `wiki/log.md`에 `lint`로 기록한다
- 주의사항 표가 빈 도구 / `status: 운영중`인데 `updated`가 오래된 도구 / 깨진 코드 링크
- 같은 `task`인데 서로 링크 안 된 도구 / 본문에만 있고 `concepts/`에 없는 용어
- `data: 민감|개인정보포함`인데 주의사항이 허술한 도구

## LLM이 지킬 것

1. **`raw/`는 읽기 전용.**
2. **모르면 모른다고 쓴다.** 코드나 문서에 없는 작동방식을 추정으로 채우지 않는다. 추정이 필요하면 `> [추정]`으로 표시한다.
3. **주의사항의 "남은 위험"을 비워두지 않는다.** 없으면 왜 없는지 쓴다.
4. **개인정보·비밀키를 위키 본문에 옮기지 않는다.**
5. **비-AI 대안을 먼저 검토한다.** 자동화 위키가 AI 사용을 부추기는 도구가 되지 않게.
6. **이 위키는 AI 활용을 권장하지 않는다.** 원본 가이드가 명시한 입장이다. 도구를 소개할 때 "쓰지 않는 선택"도 정당한 결론으로 남겨둔다.
7. **실패담을 지우지 않는다.** 중단한 도구, 잘 안 된 이식 기록이 특히 가치 있다.
8. **조직의 정책적 판단은 사람의 몫이다** — 승인 여부, 허용 범위, 책임자 지정.
